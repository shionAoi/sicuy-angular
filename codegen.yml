overwrite: true
schema: "schema.graphql"
#documents: "src/**/*.graphql"
generates:
  src/api/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
